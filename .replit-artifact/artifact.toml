kind = "web"
previewPath = "/"
title = "Blackjack"
version = "1.0.0"
id = "artifacts/blackjack"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20963

[services.development]
run = "pnpm --filter @workspace/blackjack run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/blackjack", "run", "build" ]
publicDir = "artifacts/blackjack/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20963"
BASE_PATH = "/"
